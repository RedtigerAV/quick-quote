import { createReducer } from '@ngrx/store';
import { IBackgroundImagesState, initialState } from './background-images.state';

// TODO: fill reducer with handlers
export const backgroundImagesReducer = createReducer<IBackgroundImagesState>(initialState);
