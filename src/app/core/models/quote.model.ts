export interface IQuote {
  id: string;
  quote: string;
  authorId: string;
  authorName: string;
  authorProfession?: string;
  authorNationality?: string;
  authorBorn?: string;
  authorDied?: string;
}
